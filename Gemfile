# frozen_string_literal: true

source "https://rubygems.org"

# Plain Jekyll — this site uses custom layouts, no theme.
gem "jekyll", "~> 4.3"

platforms :windows, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end
