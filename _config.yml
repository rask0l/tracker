# Personal tracker — gym + CWES study. Private-ish: noindex, not linked from the writeup site.
title: rask0l
tagline: gym + study progress
description: Personal gym and CWES study tracker.
lang: en
timezone: Asia/Phnom_Penh

# Deployed as a GitHub Pages project site at rask0l.github.io/tracker
url: "https://rask0l.github.io"
baseurl: "/tracker"

# Keep the whole site out of search engines.
defaults:
  - scope:
      path: ""
    values:
      noindex: true
      sitemap: false

sass:
  style: compressed

# Keep docs and tooling out of the built site. Note this only stops them
# being served at the URL; while the repo is public they are still readable
# on GitHub itself.
exclude:
  - CLAUDE.md
  - Gemfile
  - Gemfile.lock
  - README.md
  - vendor
  - .claude
